import { MainWindow } from "./MainWindow";
export const Main = () => {
    return (
        <div className={`container m-auto relative h-[90vh] mt-5`}>
            <MainWindow/>
        </div>
    );
};