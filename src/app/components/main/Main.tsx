import { MainWindow } from "./MainWindow";
export const Main = () => {
    return (
        <div className={`container m-auto relative h-full border`}>
            <MainWindow/>
        </div>
    );
};